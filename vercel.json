{
  "buildCommand": "corepack enable && corepack prepare pnpm@8.15.0 --activate && cd apps/web && pnpm build",
  "devCommand": "corepack enable && corepack prepare pnpm@8.15.0 --activate && cd apps/web && pnpm dev",
  "installCommand": "corepack enable && corepack prepare pnpm@8.15.0 --activate && pnpm install",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next",
  "regions": ["iad1"],
  "functions": {
    "apps/web/src/app/api/**/*.ts": {
      "maxDuration": 60,
      "memory": 1024
    }
  },
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-store, must-revalidate"
        }
      ]
    }
  ]
}
